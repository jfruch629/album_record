<div class="album">
 <h1>All Albums</h1>
 <hr>

 <table>
   <thead>
     <tr>
       <th colspan="3"></th>
     </tr>
   </thead>

   <tbody>
     <% @albums.each do |album| %>
       <tr>
         <td><%= link_to album.title, admin_album_path(album), id: album.title %> |
              <%= link_to 'Delete', album, method: :delete, data: { confirm: 'Are you sure?' }, id: 'delete' %> </td>
       </tr>
     <% end %>
   </tbody>
 </table>
</div>

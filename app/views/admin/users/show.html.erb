<div class="user">
  <h1>USER: <%= @user.first_name %> <%= @user.last_name %></h1><br /><hr><br />
  <strong>EMAIL: <%= @user.email %></strong><br /><br />

  <strong>Admin Status: </strong>
  <% if @user.admin? %>
    User is Admin
  <% else %>
    User is <i>not</i> Admin
  <% end %>

<br /><br />
  <%= link_to 'Update', edit_admin_user_path(@user) %> |
  <%= link_to 'Back', admin_users_path(@users) %> |
  <%= link_to 'Delete', @album, method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
<%= link_to 'All Users', admin_users_path, id: 'back-link'%>
